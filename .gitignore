node_modules/
coverage/
dist/

# Panda CSS output. Generated by `npm run panda:build`; never committed —
# it is derived from panda.config.ts and would go stale silently.
styled-system/

*.tsbuildinfo
.DS_Store
*.log
.env
.env.*

# Playwright
test-results/
playwright-report/
blob-report/
playwright/.cache/

# PRDs live in the INTERNAL stonedog-prd repo, never here. `docs/prd` is a
# symlink into it, so the documents are where a person expects while this
# repository commits none of them. See the StoneDogCode CLAUDE.md.
docs/prd
